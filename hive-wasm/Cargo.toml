[package]
name = "hive-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
libp2p = { version = "0.53", features = ["wasm-bindgen", "websocket-websys", "gossipsub", "noise", "yamux", "macros"] }
hive-core = { path = "../hive-core" }
